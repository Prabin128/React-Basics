# JavaScript Expressions and ES6 Template Literals in

Using JSX, we were able to insert HTML into JavaScript.

JSX also lets us add HTML inside a JavaScript file and then insert JavaScript inside that HTML,



```js
    function App() {
            return (
            <>
                <h1>Hello Mr. Developer</h1>
            </>
        )
    }
```

```What if we want to insert data stored in the variable into the html tag??```

- We have a const called nameOfTheDeveloper that we want to insert in html.  
- Instead of writig name directly to the ***<h1>*** tag, we can insert javascript code inside html, simply wrapping the JavaScript inside a set of curly braces. 

```js
function App() {
    const nameOfTheDeveloper =  'MR. Heavy Developer';
        return (
        <>
            <h1>Hello {name}</h1>
        </>
    )
}
```
***Note:*** If we miss the curly braces then it will just interpret as a string because we're inside a JSX file.


``` Similarly we can perform a javascript code inside the set of curly braces ```
```js
function App() {
    const nameOfTheDeveloper =  'MR. Heavy Developer';
    const luckyNumber = 10;
        return (
        <>
            <h1>Hello {name}</h1>
            <p> Your age must be {20+4} and your lucky number is {luckyNumber}</p>
            <p>Let's Generate the random  number using math function: {Math.floor(Math.random()*10)}</p>
        </>
    )
}
```


# Limitations

We can add any JavaScript expression in between these curly braces which is injecting code into our HTML elements in our JSX file but we can't write JavaScript statements.  The code example down below is invalid and throws an error: 

```js
function App() {
    const nameOfTheDeveloper =  'MR. Heavy Developer';
    const luckyNumber = 10;
    return (
        <>
            <p> Check if you are elligible to vote or not:  {
                if (age == 21){
                    return `you can vote`;
                }else {
                    return `you cannot vote`;
                }
            }</p>
        </>
    )
}
```

```This doesn't work because this is actually a statement rather than an expression.```

 Difference between expressions and statements is :
- expression will be evaluated to a value, right? Like it ends up, after all the code's been executed,it ends up equaling something. 
- but the above statement is actually asking the computer to do some work to evaluate this statement and then depending on that statement workout something.


#Template Literals in JSX

### What is Template Literals? 
Template literals (also known as template strings) in ES6 are a feature in JavaScript that allows for string interpolation, multi-line strings, and more flexible string creation. They are enclosed by backticks ***(`)*** instead of the usual single ***(')*** or double ***(")*** quotes used for strings.

Here’s a breakdown of what template literals can do:

### 1.String Interpolation:
Template literals allow you to embed expressions inside a string. This is done using ${} syntax.
Example:

```js
const name = 'Alice';
const age = 25;

const message = `Hello, my name is ${name} and I am ${age} years old.`;
console.log(message); // Output: Hello, my name is Alice and I am 25 years old.
```
### 2.Multi-line Strings:
Template literals allow us to create strings that span multiple lines without the need for concatenation or escape characters like ***\n***.
Example:

```js
const message = `This is a multi-line
string using template literals.
It preserves the line breaks.`;
console.log(message);
```
### 3.Expression Substitution
We can embed more complex expressions within ***${}***. These expressions can be calculations, function calls, or any valid JavaScript expression..

Example:

```js
const x = 5;
const y = 10;

const result = `The sum of ${x} and ${y} is ${x + y}.`;
console.log(result); // Output: The sum of 5 and 10 is 15.

```
Here, ***${x + y}*** is an expression that gets evaluated and inserted into the string.      

### 3.Tagged Templates
Tagged templates allow you to parse a template literal with a function. This gives you more control over the string processing.

Example:

```js
function tag(strings, ...values) {
console.log(strings); // Array of string literals
console.log(values);  // Array of substituted values
return 'Tagged Template';
}

const name = 'Alice';
const age = 25;

const result = tag`Hello, my name is ${name} and I am ${age} years old.`;
console.log(result); 
```
```Output```
```
['Hello, my name is ', ' and I am ', ' years old.']
['Alice', 25]
Tagged Template
```

#### Summary:

    - Template literals use backticks (`) to define strings.
    - You can insert expressions using ${} syntax.
    - They support multi-line strings without needing escape characters.
    - They can be used with tagged templates for more advanced string manipulation.

Template literals make string handling much easier and more readable compared to traditional string concatenation.




### Using Template Literals in React

Template Literals in ES6 basically injects strings into a piece of JavaScript.

```js
function App() {
    const firstName =  'Donald';
    const lastName = 'Trump';
    return (
        <>
            <h1>Hello my name is {`${firstName} ${lastName}`}</h1>
        </>
    )
}
```

- We've got ***`${firstName} ${lastName}`*** interpreted as a string using the ES6 template literals
- then we've got string as ***{***`${firstName} ${lastName}`***}*** being inserted in as JavaScript in these curly braces from JSX, and
- then ***Hello my name is {`${firstName} ${lastName}`}***  being inserted as a string into ***h1*** 
- and then Finally, the entire JSX expression, including the JavaScript code, is being rendered and executed within a JavaScript file..